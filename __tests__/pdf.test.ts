describe("The PDF Class", () => {
	describe("getPDF", () => {
		it("Should resolve on pdfParser_dataReady", () => {});
		it("Should reject on pdfParser_dataError", () => {});
		it("Should reject if result is empty");
	});
	describe("getShifts", () => {
		it("Produces array of shift objects from pdf json", () => {});
	});
	describe("getDaily", () => {
		it("rejects if getPDF fails", () => {});
		it("Sorts shifts into correct day", () => {});
	});
});
